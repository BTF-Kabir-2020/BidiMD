# Sample: Persian-dominant Markdown (expect RTL preview with BidiMD Auto)

# تست فارسی BidiMD

این یک پاراگراف **فارسی** است برای Preview راست‌چین.

- مورد یک
- مورد دو با `inline code` که باید LTR بماند

```ts
const hello = "code stays LTR";
console.log(hello);
```

جدول:

| ستون | مقدار |
|------|--------|
| سلام | دنیا |

English sentence mixed in — still mostly RTL.
